# main.py

import adder

value = adder.add(2, 2)

print(value)

double_value = adder.double(value)

print(double_value)

# The chapter also covers 'import <module> as <other_name>' and 'from <module> as <name>' but
# but I have not included any changes to the program to reflect that.